<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Review Warnings"
        Width="720" Height="560"
        MinWidth="560" MinHeight="400"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        Background="{DynamicResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Brand accent strip along the bottom edge -->
        <Border Grid.Row="1" Height="4" Background="{DynamicResource HeaderGradientBrush}"/>

        <Grid Grid.Row="0" Margin="16,12,16,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Warning types" Style="{DynamicResource HeadingSmallStyle}"
                           Foreground="{DynamicResource PrimaryBrush}"/>
                <TextBlock Grid.Column="1" x:Name="lbl_count" Text="0/0"
                           VerticalAlignment="Bottom"
                           Style="{DynamicResource SecondaryTextSmallStyle}"
                           ToolTip="Checked / all warning types"/>
            </Grid>

            <TextBlock Grid.Row="1" Text="Filter warning types" Style="{DynamicResource CaptionStyle}" Margin="0,10,0,3"/>
            <TextBox Grid.Row="2" x:Name="tb_filter" TextChanged="filter_changed"
                     ToolTip="Show only warning types whose text contains this"/>

            <ListBox Grid.Row="3" x:Name="list_warnings" Margin="0,8,0,0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     VirtualizingStackPanel.IsVirtualizing="True">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox IsChecked="{Binding checked}"
                                  Checked="item_checked" Unchecked="item_unchecked"
                                  VerticalContentAlignment="Center"
                                  ToolTip="{Binding Label}">
                            <TextBlock Text="{Binding Label}" TextWrapping="Wrap"/>
                        </CheckBox>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Grid Grid.Row="4" Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="Check All" Click="check_all"
                        Style="{DynamicResource SmallButtonStyle}" Margin="0,0,6,0"/>
                <Button Grid.Column="1" Content="Check None" Click="check_none"
                        Style="{DynamicResource SmallButtonStyle}"/>
            </Grid>

            <!-- Bottom bar -->
            <Grid Grid.Row="5" Margin="0,14,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Image x:Name="brand_logo" Height="24" Width="24" VerticalAlignment="Center"/>
                    <TextBlock x:Name="brand_wordmark" Text="BIMINENT" Margin="8,0,0,0"
                               VerticalAlignment="Center"
                               FontFamily="{DynamicResource PrimaryFontFamily}"
                               FontSize="13" FontWeight="SemiBold"
                               Foreground="{DynamicResource PrimaryBrush}"/>
                    <TextBlock x:Name="status_text" Text="" Margin="14,0,0,0"
                               VerticalAlignment="Center"
                               Style="{DynamicResource SecondaryTextSmallStyle}"
                               TextTrimming="CharacterEllipsis"/>
                </StackPanel>
                <Button Grid.Column="1" Content="Close" Click="button_close"
                        Style="{DynamicResource GhostButtonStyle}"/>
                <Button Grid.Column="3" x:Name="btn_isolate" Content="Isolate in view"
                        Click="do_isolate" IsEnabled="False"
                        Style="{DynamicResource SecondaryButtonStyle}"
                        ToolTip="Temporarily isolate the involved elements in the active view"/>
                <Button Grid.Column="5" x:Name="btn_select" Content="Select"
                        Click="do_select" IsDefault="True" IsEnabled="False"
                        Style="{DynamicResource PrimaryButtonStyle}"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
