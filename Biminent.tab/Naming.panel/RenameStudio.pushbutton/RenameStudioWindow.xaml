<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename Studio"
        Width="820" Height="680"
        MinWidth="680" MinHeight="480"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        Background="{DynamicResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Brand accent strip along the bottom edge -->
        <Border Grid.Row="1" Height="4" Background="{DynamicResource HeaderGradientBrush}"/>

        <Grid Grid.Row="0" Margin="16,12,16,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: controls -->
            <StackPanel Grid.Column="0">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Scope" Style="{DynamicResource HeadingSmallStyle}" Foreground="{DynamicResource PrimaryBrush}"/>
                    <Button Grid.Column="1" Content="&#x2699;" Click="open_settings"
                            Style="{DynamicResource GhostButtonStyle}"
                            Padding="8,2" FontSize="14"
                            ToolTip="Choose which categories appear in the dropdown"/>
                </Grid>

                <TextBlock Text="Category" Style="{DynamicResource CaptionStyle}" Margin="0,10,0,3"/>
                <ComboBox x:Name="cmb_scope" SelectionChanged="scope_changed"/>

                <TextBlock Text="Rename rules" Style="{DynamicResource HeadingSmallStyle}" Foreground="{DynamicResource PrimaryBrush}" Margin="0,24,0,0"/>

                <TextBlock Text="Find" Style="{DynamicResource CaptionStyle}" Margin="0,10,0,3"/>
                <TextBox x:Name="tb_find" TextChanged="rules_changed"/>

                <TextBlock Text="Replace with" Style="{DynamicResource CaptionStyle}" Margin="0,8,0,3"/>
                <TextBox x:Name="tb_replace" TextChanged="rules_changed"/>

                <Grid Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Prefix" Style="{DynamicResource CaptionStyle}" Margin="0,0,0,3"/>
                    <TextBlock Grid.Row="0" Grid.Column="2" Text="Suffix" Style="{DynamicResource CaptionStyle}" Margin="0,0,0,3"/>
                    <TextBox Grid.Row="1" Grid.Column="0" x:Name="tb_prefix" TextChanged="rules_changed"/>
                    <TextBox Grid.Row="1" Grid.Column="2" x:Name="tb_suffix" TextChanged="rules_changed"/>
                </Grid>

                <TextBlock Text="Rules apply to every checked element and preview live on the right. To rename a single element, just type its new name directly in the right-hand column."
                           Style="{DynamicResource SecondaryTextSmallStyle}"
                           TextWrapping="Wrap" Margin="0,16,0,0"/>
            </StackPanel>

            <!-- Right: preview -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Preview" Style="{DynamicResource HeadingSmallStyle}" Foreground="{DynamicResource PrimaryBrush}"/>
                    <TextBlock Grid.Column="1" x:Name="lbl_count" Text="0/0"
                               VerticalAlignment="Bottom"
                               Style="{DynamicResource SecondaryTextSmallStyle}"
                               ToolTip="Checked / all elements"/>
                </Grid>

                <TextBlock Grid.Row="1" Text="Search elements" Style="{DynamicResource CaptionStyle}" Margin="0,10,0,3"/>
                <TextBox Grid.Row="2" x:Name="tb_filter" Margin="0,0,0,8"
                         TextChanged="filter_changed"
                         ToolTip="Show only elements whose current name contains this text"/>

                <ListBox Grid.Row="3" x:Name="list_items"
                         SelectionMode="Extended"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         VirtualizingStackPanel.IsVirtualizing="True">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="28"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="26"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <CheckBox Grid.Column="0" IsChecked="{Binding checked}"
                                          Checked="item_checked" Unchecked="item_unchecked"
                                          VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="{Binding OldName}"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           TextTrimming="CharacterEllipsis"
                                           ToolTip="{Binding OldName}"
                                           VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="2" Text="&#x2192;"
                                           Foreground="{DynamicResource TextDisabledBrush}"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <TextBox Grid.Column="3" Text="{Binding NewName, Mode=OneWay}"
                                         TextChanged="newname_edited"
                                         Padding="6,3" Margin="0,1"
                                         ToolTip="New name - type here to rename this element directly"
                                         VerticalAlignment="Center" VerticalContentAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Grid Grid.Row="4" Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="Check All" Click="check_all"
                            Style="{DynamicResource SmallButtonStyle}" Margin="0,0,6,0"
                            ToolTip="Check all visible elements"/>
                    <Button Grid.Column="1" Content="Check None" Click="uncheck_all"
                            Style="{DynamicResource SmallButtonStyle}"
                            ToolTip="Uncheck all visible elements"/>
                </Grid>
            </Grid>
        </Grid>

        <!-- Bottom bar -->
        <Grid Grid.Row="1" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Image x:Name="brand_logo" Height="24" Width="24" VerticalAlignment="Center"/>
                <TextBlock x:Name="brand_wordmark" Text="BIMINENT" Margin="8,0,0,0"
                           VerticalAlignment="Center"
                           FontFamily="{DynamicResource PrimaryFontFamily}"
                           FontSize="13" FontWeight="SemiBold"
                           Foreground="{DynamicResource PrimaryBrush}"/>
                <TextBlock x:Name="status_text" Text="" Margin="14,0,0,0"
                           VerticalAlignment="Center"
                           Style="{DynamicResource SecondaryTextSmallStyle}"
                           TextTrimming="CharacterEllipsis"/>
            </StackPanel>
            <Button Grid.Column="1" Content="Close" Click="button_close"
                    Style="{DynamicResource GhostButtonStyle}"/>
            <Button Grid.Column="3" x:Name="btn_rename" Content="Rename"
                    Click="do_rename" IsDefault="True" IsEnabled="False"
                    Style="{DynamicResource PrimaryButtonStyle}"/>
        </Grid>
        </Grid>
    </Grid>
</Window>
